import React from 'react';

const ExperiencePageLayout = ( { children } ) => {
    return ( 
        <section>
            {children}
        </section>    
     );
}
 
export default ExperiencePageLayout;